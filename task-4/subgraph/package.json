{
  "scripts": {
    "prepare": "graph codegen && graph build"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.71.2",
    "@graphprotocol/graph-ts": "^0.35.1"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
